# SDK Test Configuration

# Private key for test wallet (must have test USDC on Base Sepolia)
# Format: 0x followed by 64 hex characters
TEST_WALLET_PRIVATE_KEY=0x...

# API URL for integration tests (optional, defaults to production)
TEST_API_URL=https://api.llm-fid.fun

# Local API URL for development testing (optional)
TEST_LOCAL_API_URL=http://localhost:8787

# Skip integration tests (set to "true" to run only unit tests)
SKIP_INTEGRATION_TESTS=false
